# What happens when „1‟== 1 is executed?

result = "1" == 1
print(result)

# when we try to compare string with intiger it gives a false answer bcz  string and intiger are different datatypes so it gives false 

# "1" == 1 results in False because a string is not equal to an integer.